# Database (Neon Postgres)
DATABASE_URL=

# Session (generate: openssl rand -base64 32)
SESSION_SECRET=some-long-random-32+character-string

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
